import 'package:flutter/material.dart';

class AnamneseAnswerPage extends StatelessWidget {
  const AnamneseAnswerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Pergunta'),
      ),
      body: Container(),
    );
  }
}
